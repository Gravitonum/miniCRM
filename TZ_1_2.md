# Техническое задание (ТЗ)
## Разработка мини-CRM «GraviSales» на платформе GraviBase

| Параметр | Значение |
|---|---|
| Версия | 1.3 |
| Дата | 19 февраля 2026 |
| Платформа | GraviBase (https://gravibase.ru) |

---

## Журнал изменений

### v1.0 → v1.1

| # | Раздел | Правка |
|---|---|---|
| 1 | 3.4 | Устранено противоречие «автоматическая фиксация (вручную)» |
| 2 | 3.2 | Разделены организационно-правовая форма и тип отношений с клиентом |
| 3 | 4.6 | Контакты: одиночный FK → таблица-связка `contact_company_links` (many-to-many) |
| 4 | 4.x | Добавлены 4 недостающие таблицы: `interactions`, `deal_stage_history`, `deal_products`, `tags` |
| 5 | 4.7 | Теги сделок: `text[]` заменён на корректную связку `deal_tags` |
| 6 | 2.3 | Добавлен fallback-план для email-нотификаций |
| 7 | 2.3 | Уточнён механизм PDF-экспорта |
| 8 | 5.5 | Вкладка «Выручка» ↔ раздел 3.6 приведены к единому наименованию |
| 9 | 5.x | Добавлены 2 недостающих экрана: онбординг и управление пользователями |
| 10 | НФТ | Добавлен раздел нефункциональных требований |

### v1.1 → v1.2

| # | Раздел | Правка |
|---|---|---|
| 1 | 2.2 | Ролевая модель разделена на два уровня: платформа (SuperAdmin) и тенант (CompanyAdmin, Manager, Viewer) |
| 2 | 3.1 | Уточнён процесс регистрации тенанта: самостоятельная публичная регистрация, SuperAdmin не участвует |
| 3 | 4.1 | Добавлена таблица `platform_admins` — отдельная от `company_id`-модели |
| 4 | 4.x | Добавлена таблица `company_invites` — управление приглашениями пользователей внутри тенанта |
| 5 | 5.0 | Онбординг уточнён: описана публичная страница регистрации + визард настройки компании |
| 6 | 5.2 | Экран управления пользователями уточнён: механизм invite-by-email |
| 7 | 5.x | Добавлен экран платформенной панели SuperAdmin |

### v1.2 → v1.3

| # | Раздел | Правка |
|---|---|---|
| 1 | 2.1 | Добавлено требование PWA (Progressive Web App): manifest, service worker, offline-заглушка |
| 2 | 2.4 | Новый раздел: мультиязычность (i18n) — поддержка нескольких языков интерфейса |
| 3 | 2.5 | Новый раздел: система тем оформления (dark / light / system) |
| 4 | 4.1 | `companies` — добавлено поле `default_language` |
| 5 | 4.15 | Новая таблица `user_preferences` — пользовательские настройки (язык, тема) |
| 6 | 5.x | UI: переключатель языка и темы в хедере приложения |
| 7 | 5.0 | Онбординг: добавлен шаг выбора языка интерфейса |
| 8 | 6 | НФТ: требования PWA (Lighthouse ≥ 90), i18n, поддержка тем |

---

## 1. Цель и назначение системы

### 1.1. Цель проекта

Разработка простого и удобного SaaS-решения (мини-CRM) для управления B2B-сделками, воронками продаж и клиентской базой. Система предназначена для малого бизнеса любого профиля (услуги, оптовая торговля, производство).

### 1.2. Задачи системы

- Централизованное хранение данных о клиентах (компании и контактные лица).
- Управление сделками с использованием гибких воронок продаж.
- Автоматизация рутинных процессов (импорт/экспорт, нотификации).
- Предоставление аналитики по продажам (конверсия, выручка).
- Обеспечение разграничения прав доступа (Администратор, Менеджер, Наблюдатель).

---

## 2. Общие требования к системе

### 2.1. Требования к архитектуре

- **Платформа:** бэкенд и хостинг на BaaS-платформе GraviBase.
- **Фронтенд:** веб-интерфейс, адаптивный (desktop-first, с поддержкой планшетов). Приложение реализуется как **PWA (Progressive Web App)**: `manifest.json`, service worker, offline-заглушка, иконки для установки на устройство. Мобильная версия — в следующем релизе (отдельное ТЗ).
- **Мультитенантность:** реализуется на уровне БД — поле `company_id` (UUID) во всех таблицах с пользовательскими данными.

### 2.2. Ролевая модель

Система имеет **два уровня администрирования**, которые принципиально разделены.

#### Уровень 1 — Платформа

Это команда-владелец продукта GraviSales. Не использует роли GraviBase — управляется через отдельную таблицу `platform_admins`.

| Роль | Кто | Права |
|---|---|---|
| **SuperAdmin** | Владелец/команда продукта | Просмотр списка тенантов и их метаданных (название, статус, дата регистрации). Блокировка/разблокировка тенанта. Управление тарифами. **Не имеет доступа к бизнес-данным внутри тенантов** (сделки, клиенты, контакты — недоступны) |

> SuperAdmin работает в отдельном интерфейсе (платформенная панель), недоступном обычным пользователям.

#### Уровень 2 — Тенант (внутри каждой компании)

Используется встроенный механизм ролей GraviBase. Каждый пользователь привязан к конкретному `company_id` и видит **только данные своей компании**.

| Роль | Кто назначает | Права |
|---|---|---|
| **CompanyAdmin** | Создаётся автоматически при регистрации тенанта (первый пользователь). Может быть передана другому пользователю | Полный доступ к данным своей компании: настройки, пользователи, справочники, воронки, все сделки и клиенты |
| **Manager** | CompanyAdmin | Создание/редактирование клиентов и сделок, просмотр отчётов. Без доступа к системным настройкам и конфигурации воронок |
| **Viewer** | CompanyAdmin | Только просмотр всех разделов своей компании |

> Пользователь с любой ролью внутри тенанта **не знает о существовании других тенантов** и не имеет доступа к их данным.

### 2.3. Технические требования к реализации на GraviBase

**Email-нотификации:** использовать запланированный функционал платформы. Триггеры: смена ответственного за сделку, дедлайн через N дней (настраивается). **Fallback (если функционал GraviBase не выпущен в срок):** реализовать отправку через внешний SMTP-сервис по webhook-вызову из GraviBase, либо отложить модуль нотификаций до выхода платформенного функционала — с явным указанием в release notes.

**Импорт/Экспорт:** загрузка/выгрузка файлов CSV и Excel с обработкой на стороне GraviBase.

**PDF-экспорт:** реализовать на стороне клиента через браузерный `window.print()` с CSS-стилями для печати. Серверный рендеринг не предусмотрен в рамках данного ТЗ. Для сложных отчётов допускается использование клиентской библиотеки (jsPDF или аналог).

### 2.4. Мультиязычность (i18n)

Система поддерживает несколько языков интерфейса. Язык по умолчанию — **русский (ru)**.

- **Поддерживаемые языки на старте:** русский (`ru`), английский (`en`). Архитектура должна допускать добавление новых языков без изменения кода — через файлы переводов.
- **Механизм переключения:** выпадающий список в хедере приложения (иконка глобуса + текущий язык).
- **Хранение выбора:** персональный язык хранится в таблице `user_preferences.language`. Если у пользователя нет записи — используется `companies.default_language`, затем fallback на `ru`.
- **Реализация:** клиентская i18n-библиотека (например, `react-i18next` или `vue-i18n`). Все строки интерфейса выносятся в JSON-файлы переводов (`locales/ru.json`, `locales/en.json`).
- **Серверные данные (справочники, названия этапов и т.д.):** остаются на языке ввода — перевод серверных данных не предусмотрен.

### 2.5. Поддержка тем оформления

Система поддерживает переключение между **светлой** и **тёмной** темами.

- **Доступные темы:** `light` (светлая), `dark` (тёмная), `system` (следует настройкам ОС через `prefers-color-scheme`).
- **Тема по умолчанию:** `system`.
- **Механизм переключения:** иконка sun/moon в хедере приложения с выпадающим меню из трёх вариантов.
- **Хранение выбора:** в таблице `user_preferences.theme`. Если у пользователя нет записи — используется `system`.
- **Реализация:** CSS custom properties (design tokens) для всех цветов. Класс `dark` / `light` на `<html>`. Все компоненты используют семантические токены (`--color-bg-primary`, `--color-text-primary`, и т.д.), а не фиксированные цвета.
- **Печать (PDF-экспорт):** при печати всегда применяется светлая тема через `@media print`.

---

## 3. Функциональные требования (модули системы)

### 3.1. Управление компанией (Тенант)

**Регистрация тенанта — самостоятельная, через публичную форму.** SuperAdmin в процессе регистрации не участвует. При регистрации автоматически:
- создаётся запись в таблице `companies`,
- создаётся первый пользователь с ролью `CompanyAdmin`.

**Онбординг-визард** после первой авторизации: название компании, валюта, часовой пояс (можно пропустить и заполнить позже).

**Настройки компании** (доступны CompanyAdmin): название, логотип, часовой пояс, валюта по умолчанию, API-ключ Контур-Фокус.

**Управление пользователями** (доступно CompanyAdmin):
- Приглашение коллег по email с выбором роли (CompanyAdmin / Manager / Viewer).
- Пользователь получает письмо со ссылкой → переходит → задаёт пароль → автоматически привязывается к компании с заданной ролью.
- Самостоятельная регистрация без приглашения — недоступна.
- Деактивация пользователя без удаления его данных.

**Блокировка тенанта** производится только SuperAdmin через платформенную панель (например, за неоплату). Заблокированный тенант: пользователи видят страницу-заглушку, данные сохраняются.

### 3.2. Справочники системы

Все справочники управляются через единый раздел «Настройки → Справочники».

- **Источники лидов** — откуда пришёл клиент (Сайт, Звонок, Рекомендация и т.д.).
- **Организационно-правовая форма (ОПФ)** — юридический тип клиента: ООО, АО, ИП, Физлицо и т.д. *(Исправлено: выделено в отдельный справочник, не смешивается с типом отношений.)*
- **Тип отношений с клиентом** — Клиент, Партнёр, Конкурент, Потенциальный. *(Исправлено: отдельный справочник.)*
- **Категории продуктов** — номенклатура для привязки к сделкам.
- **Теги** — свободные текстовые метки для гибкой фильтрации клиентов и сделок (хранятся в отдельной таблице `tags`, связь через `deal_tags` и `client_tags`).

### 3.3. Модуль «Воронки продаж»

- Создание нескольких воронок (пример: «Основная», «Возврат клиентов»).
- Настройка этапов: CompanyAdmin может добавлять, удалять и менять порядок этапов.
- Каждый этап имеет тип: `open` (В работе), `won` (Закрыто успешно), `lost` (Закрыто неуспешно).
- Воронку с существующими сделками нельзя удалить — только деактивировать.

### 3.4. Модуль «Клиенты»

**Карточка компании-клиента:**
- Название, ИНН, ОПФ, Тип отношений, Источник лида, Адрес, Сайт.
- Кнопка «Загрузить из Контур-Фокус» по ИНН — подтягивает реквизиты через API, данные кешируются в поле `kontur_data` (JSONB). Требует наличия API-ключа Контур-Фокус (ключ хранится в настройках тенанта; каждая компания использует собственный ключ). При ошибке API — показывается уведомление, ручной ввод остаётся доступным.
- Теги.

**Карточка контактного лица:**
- ФИО, Должность, Рабочий телефон, Личный телефон, Email, Комментарий.
- Контакт может быть привязан к **одной или нескольким** компаниям-клиентам (связь через таблицу `contact_company_links`).

**История взаимодействий** *(Исправлено: убрано слово «автоматическая» — фиксация ручная):*
- Менеджер вручную добавляет записи: звонок, встреча, email, заметка.
- Каждая запись содержит: тип, дату/время, текст, автора.
- Лента истории доступна как в карточке клиента, так и в карточке сделки.

### 3.5. Модуль «Сделки»

- Карточка сделки привязана к компании-клиенту и (опционально) к контактному лицу.
- Поля: Название, Сумма, Валюта (из настроек тенанта), Ответственный, Дедлайн, Воронка, Текущий этап, Теги.
- История перемещения по этапам (аудит) — автоматически фиксируется при каждом изменении этапа.
- Привязка продуктов из справочника категорий (через таблицу `deal_products`).
- История взаимодействий в контексте сделки.

### 3.6. Модуль «Отчёты»

- **Воронка (Конверсия):** визуальное отображение количества и суммы сделок на каждом этапе, процент конверсии между этапами.
- **По менеджерам:** сумма и количество выигранных сделок за период в разрезе ответственных.
- **По воронкам:** сравнение эффективности нескольких воронок за период.

Все отчёты поддерживают фильтр по диапазону дат.

### 3.7. Импорт и экспорт данных

- **Импорт:** загрузка CSV/XLSX для создания или обновления компаний-клиентов и контактов. Маппинг колонок задаётся пользователем. Режимы: «Создать новых» / «Обновить по ИНН».
- **Экспорт:** выгрузка таблиц (Клиенты, Сделки) в Excel; отчётов в PDF (через браузерную печать).

---

## 4. Структура базы данных (GraviBase)

Все таблицы находятся в схеме `public`. Служебные поля платформы (`id UUID PK`, `created_at`, `updated_at`) присутствуют во всех таблицах и далее не перечисляются. Поле `company_id UUID` — обязательный внешний ключ на `companies.id` для изоляции тенантов.

---

### 4.0. `platform_admins` — администраторы платформы

> Таблица **не содержит** `company_id` — это отдельный уровень, не связанный с тенантами.

| Поле | Тип | Описание |
|---|---|---|
| `email` | text | Email SuperAdmin |
| `password_hash` | text | Хеш пароля |
| `name` | text | Имя |
| `is_active` | bool | Активен ли аккаунт |

---

### 4.1. `companies` — тенанты (CRM-компании)

| Поле | Тип | Описание |
|---|---|---|
| `name` | text | Название CRM-компании |
| `timezone` | text | Часовой пояс (например, `Europe/Moscow`) |
| `currency` | text | Валюта по умолчанию (`RUB`) |
| `default_language` | text | Язык интерфейса по умолчанию для тенанта (`ru`, `en`). Default: `ru` |
| `logo_url` | text | URL логотипа |
| `kontur_api_key` | text | API-ключ Контур-Фокус (зашифрован) |
| `is_blocked` | bool | Заблокирован ли тенант (управляется SuperAdmin) |
| `blocked_reason` | text | Причина блокировки |

> Таблица **не** содержит `company_id` — это корневая сущность тенанта.

---

### 4.2. `crm_funnels` — воронки продаж

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies |
| `name` | text | Название воронки |
| `is_active` | bool | Активна ли воронка |

---

### 4.3. `funnel_stages` — этапы воронок

| Поле | Тип | Описание |
|---|---|---|
| `funnel_id` | UUID | FK → crm_funnels |
| `name` | text | Название этапа |
| `order` | int | Порядок отображения |
| `status_type` | text | `open` / `won` / `lost` |

---

### 4.4. `directories` — единая таблица справочников

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies |
| `type` | text | `lead_source`, `legal_form`, `client_relation`, `product_category` |
| `value` | text | Значение элемента |
| `is_active` | bool | Активен ли элемент |

> **Теги** вынесены в отдельную таблицу `tags` (см. 4.5) — у них иная логика связей.

---

### 4.5. `tags` — теги

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies |
| `name` | text | Название тега |

---

### 4.6. `client_companies` — компании-клиенты

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies (тенант) |
| `name` | text | Название компании-клиента |
| `inn` | text | ИНН |
| `legal_form_id` | UUID | FK → directories (type = `legal_form`) |
| `relation_type_id` | UUID | FK → directories (type = `client_relation`) |
| `source_id` | UUID | FK → directories (type = `lead_source`) |
| `address` | text | Адрес |
| `website` | text | Сайт |
| `kontur_data` | jsonb | Кеш данных Контур-Фокус |

---

### 4.7. `contacts` — контактные лица

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies (тенант) |
| `first_name` | text | Имя |
| `last_name` | text | Фамилия |
| `position` | text | Должность |
| `phone_work` | text | Рабочий телефон |
| `phone_personal` | text | Личный телефон |
| `email` | text | Email |
| `comment` | text | Комментарий |

---

### 4.8. `contact_company_links` — связь контактов с компаниями (many-to-many)

> **Исправлено:** в v1.0 использовался одиночный FK `client_company_id` в таблице `contacts`, что противоречило требованию 3.4 «привязка к одной или нескольким компаниям».

| Поле | Тип | Описание |
|---|---|---|
| `contact_id` | UUID | FK → contacts |
| `client_company_id` | UUID | FK → client_companies |
| `is_primary` | bool | Основное место работы |

---

### 4.9. `company_invites` — приглашения пользователей в тенант

> **Добавлено в v1.2.** Обеспечивает механизм invite-only регистрации внутри тенанта.

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies |
| `email` | text | Email приглашаемого |
| `role` | text | `company_admin` / `manager` / `viewer` |
| `token` | text | Уникальный токен ссылки приглашения |
| `invited_by` | UUID | ID пользователя GraviBase, отправившего приглашение |
| `accepted_at` | timestamp | Дата принятия (NULL = ещё не принято) |
| `expires_at` | timestamp | Срок действия ссылки |

---

### 4.9. `deals` — сделки

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies (тенант) |
| `name` | text | Название сделки |
| `client_company_id` | UUID | FK → client_companies |
| `contact_id` | UUID | FK → contacts (опционально) |
| `funnel_id` | UUID | FK → crm_funnels |
| `current_stage_id` | UUID | FK → funnel_stages |
| `amount` | numeric | Сумма |
| `responsible_user_id` | UUID | ID пользователя GraviBase |
| `deadline` | timestamp | Дедлайн |
| `closed_at` | timestamp | Дата закрытия |

> **Исправлено:** поле `tags text[]` убрано. Связь с тегами — через таблицу `deal_tags`.

---

### 4.10. `deal_tags` — теги сделок

| Поле | Тип | Описание |
|---|---|---|
| `deal_id` | UUID | FK → deals |
| `tag_id` | UUID | FK → tags |

---

### 4.11. `client_tags` — теги компаний-клиентов

| Поле | Тип | Описание |
|---|---|---|
| `client_company_id` | UUID | FK → client_companies |
| `tag_id` | UUID | FK → tags |

---

### 4.12. `deal_products` — продукты в сделке

> **Добавлено в v1.1:** в v1.0 таблица отсутствовала, хотя требование 3.5 её подразумевало.

| Поле | Тип | Описание |
|---|---|---|
| `deal_id` | UUID | FK → deals |
| `product_id` | UUID | FK → directories (type = `product_category`) |
| `quantity` | numeric | Количество |
| `price` | numeric | Цена за единицу |

---

### 4.13. `deal_stage_history` — история перемещения сделок по этапам

> **Добавлено в v1.1:** в v1.0 таблица отсутствовала, хотя аудит упомянут в разделе 3.5.

| Поле | Тип | Описание |
|---|---|---|
| `deal_id` | UUID | FK → deals |
| `from_stage_id` | UUID | FK → funnel_stages (откуда, NULL для первого этапа) |
| `to_stage_id` | UUID | FK → funnel_stages (куда) |
| `changed_by` | UUID | ID пользователя GraviBase |
| `changed_at` | timestamp | Дата/время смены |

---

### 4.14. `interactions` — история взаимодействий

> **Добавлено в v1.1:** в v1.0 таблица отсутствовала, хотя история описана в разделах 3.4 и 3.5.

| Поле | Тип | Описание |
|---|---|---|
| `company_id` | UUID | FK → companies (тенант) |
| `type` | text | `call`, `meeting`, `email`, `note` |
| `client_company_id` | UUID | FK → client_companies (опционально) |
| `deal_id` | UUID | FK → deals (опционально) |
| `contact_id` | UUID | FK → contacts (опционально) |
| `interaction_date` | timestamp | Дата/время взаимодействия |
| `description` | text | Содержание |
| `author_user_id` | UUID | ID пользователя GraviBase |

> Запись может быть привязана к клиенту, сделке или контакту — независимо.

---

### 4.15. `user_preferences` — пользовательские настройки

> **Добавлено в v1.3.** Хранит персональные предпочтения пользователей (язык, тема).

| Поле | Тип | Описание |
|---|---|---|
| `user_id` | UUID | ID пользователя GraviBase (уникальный, NOT NULL) |
| `company_id` | UUID | FK → companies (тенант) |
| `language` | text | Язык интерфейса (`ru`, `en`, ...). NULL = использовать `companies.default_language` |
| `theme` | text | Тема оформления (`light`, `dark`, `system`). NULL = `system` |

> У каждого пользователя может быть **не более одной** записи (`user_id` уникален).

---

### Схема связей (ERD-описание)

```
platform_admins          ← отдельный уровень, не связан с company_id
   └── (управляет) companies.is_blocked

companies
 ├── company_invites      ← управление приглашениями пользователей
 ├── user_preferences     ← персональные настройки (язык, тема)
 ├── crm_funnels → funnel_stages
 ├── directories (типы: lead_source, legal_form, client_relation, product_category)
 ├── tags
 ├── client_companies
 │    ├── contact_company_links ← contacts
 │    ├── client_tags ← tags
 │    └── deals
 │         ├── deal_tags ← tags
 │         ├── deal_products ← directories (product_category)
 │         ├── deal_stage_history → funnel_stages
 │         └── interactions
 └── interactions (контекст: клиент или сделка)
```

---

## 5. Описание экранных форм (UI/UX)

Общий принцип интерфейса: левый сайдбар — навигация, правая область — основной контент. Минимум модальных окон: предпочтительны сайдбар-панели для деталей. Цветовая схема реализована через **CSS-токены** и поддерживает светлую и тёмную темы (см. 2.5).

**Глобальный хедер** (правая часть) содержит:
- Переключатель языка — иконка глобуса + текущий язык, выпадающий список.
- Переключатель темы — иконка ☀️/🌙, выпадающее меню (Светлая / Тёмная / Системная).
- Аватар пользователя с меню (профиль, выход).

---

### 5.0. Публичная регистрация и онбординг тенанта

> **Обновлено в v1.2:** уточнён механизм — регистрация полностью самостоятельная.

**Публичная страница регистрации** (доступна без авторизации):
- Поля: Email, Пароль, Имя. Кнопка «Создать аккаунт».
- После успешной регистрации: автоматически создаётся тенант и пользователь с ролью `CompanyAdmin` → редирект на онбординг-визард.

**Онбординг-визард (4 шага):**
1. **Выбор языка** — выбор языка интерфейса (ru / en). Определяет язык дальнейших шагов визарда. *(Добавлено в v1.3)*
2. **Настройка компании** — название, валюта, часовой пояс, язык по умолчанию для компании.
3. **Первая воронка** — предложение создать воронку с дефолтными этапами или пропустить.
4. **Приглашение коллег** — поля email + роль (можно добавить несколько или пропустить).

После завершения: переход на Dashboard с подсказками (onboarding tour).

> Пользователи, получившие приглашение от CompanyAdmin, **не проходят** публичную регистрацию — они переходят по персональной ссылке из письма, задают только пароль и сразу оказываются внутри компании.

---

### 5.1. Dashboard (Главный экран)

**Макет:** левый сайдбар (навигация), основной контент — сетка виджетов.

**Виджеты:**
- «Мои сделки» — список активных сделок текущего пользователя с суммой и дедлайном.
- «Сделки с дедлайном на этой неделе» — список с сортировкой по срочности.
- «Выигрыши за текущий месяц» — сумма закрытых успешно сделок.
- «График активности» — количество взаимодействий по дням за последние 30 дней.

> **Исправлено:** виджет «Задачи на сегодня» удалён — модуль задач в системе отсутствует. При необходимости — вынести в отдельное ТЗ.

---

### 5.2. Управление пользователями (CompanyAdmin)

> **Обновлено в v1.2:** уточнён механизм invite-only.

**Список пользователей:** таблица с колонками Имя, Email, Роль, Статус (активен / приглашение отправлено / деактивирован), Дата добавления.

**Пригласить пользователя:**
- CompanyAdmin вводит email и выбирает роль → система создаёт запись в `company_invites` и отправляет письмо.
- Приглашение действительно 7 дней. Можно отозвать или переотправить.
- В списке пользователей приглашённый отображается со статусом «Ожидает подтверждения» до момента перехода по ссылке.

**Другие действия:**
- Смена роли через выпадающий список прямо в строке таблицы.
- Деактивация пользователя — данные (сделки, взаимодействия) сохраняются, авторизация блокируется.
- Передача роли CompanyAdmin другому пользователю (с подтверждением).

---

### 5.3. Воронки и этапы (CompanyAdmin)

**Макет:** двухколоночный. Слева — список воронок с кнопкой «+ Добавить воронку». Справа — конструктор этапов выбранной воронки.

**Конструктор этапов:**
- Этапы отображаются в виде горизонтальных плиток с drag-and-drop для изменения порядка.
- Кнопка «+ Добавить этап» → всплывающее окно: Название, Тип (open / won / lost).
- Этапы типа `won` и `lost` выделены цветом (зелёный / красный).
- Удаление этапа — только если к нему не привязаны активные сделки.

---

### 5.4. Клиенты

**Список (таблица):**
- Колонки: Название, ИНН, ОПФ, Тип отношений, Источник, Кол-во контактов, Кол-во сделок.
- Поиск (по названию, ИНН), фильтры (ОПФ, тип отношений, теги, источник).
- Кнопка «+ Добавить клиента».

**Карточка компании-клиента (4 вкладки):**

**Вкладка «Основное»:**
- Поля: Название, ИНН + кнопка 🔍 «Загрузить из Контур-Фокус», ОПФ, Тип отношений, Источник лида, Адрес, Сайт, Теги.
- Режимы просмотр / редактирование переключаются кнопкой «Редактировать».

**Вкладка «Контакты»:**
- Список контактных лиц: ФИО, Должность, Телефон, Email.
- Кнопка «+ Добавить контакт» → форма в сайдбаре.
- Клик на контакт → открытие карточки контакта.

**Вкладка «Сделки»:**
- Список связанных сделок: Название, Сумма, Этап, Ответственный, Дедлайн.
- Кнопка «+ Создать сделку» (преднаполняет поле клиента).

**Вкладка «История»:**
- Лента взаимодействий в хронологическом порядке (новые сверху).
- Кнопка «+ Добавить» → выбор типа (Звонок, Встреча, Email, Заметка), дата/время, текст.

---

### 5.5. Контактные лица

**Список:** таблица с колонками ФИО, Компании, Должность, Телефон, Email.

**Карточка контакта:**
- Основные данные + список привязанных компаний (с возможностью добавить ещё).
- История взаимодействий.

---

### 5.6. Сделки (Kanban-view)

**Макет:**
- Переключатель воронок — в шапке страницы.
- Доска: колонки = этапы выбранной воронки, горизонтальный скролл при большом числе этапов.
- Над доской: фильтр по ответственному, диапазону суммы, тегам.

**Карточка на доске** содержит: название, сумму, ответственного (аватар), дедлайн (красным если просрочен).

**Drag-and-drop:** перетаскивание карточки между колонками автоматически фиксирует смену этапа в `deal_stage_history`. При перетаскивании в колонку `won` / `lost` — всплывает подтверждение с полем «Дата закрытия».

**Сайдбар сделки** (открывается по клику):
- Все поля карточки в режиме быстрого редактирования.
- Вкладки: «Детали», «Продукты», «История».

Дополнительно доступен **табличный вид** (переключение кнопкой).

---

### 5.7. Отчёты

**Макет:** вкладки сверху.

**Вкладка «Воронка (Конверсия)»** *(Исправлено: в v1.0 называлась «Воронка»):*
- Выбор воронки и диапазона дат.
- Визуальная воронка-диаграмма (bar chart с убыванием).
- Таблица: Этап → Кол-во сделок → Сумма → Конверсия из предыдущего этапа.

**Вкладка «По менеджерам»:**
- Выбор периода.
- Таблица и круговая диаграмма: Менеджер → Кол-во выигранных сделок → Сумма.

**Вкладка «По воронкам»** *(Исправлено: в v1.0 вкладка называлась «Выручка» — переименована для соответствия разделу 3.6):*
- Сравнительная таблица воронок: Кол-во сделок, Общая сумма, % выигрышей.

Кнопка «Экспорт» на каждой вкладке — PDF (печать) / Excel.

---

### 5.8. Импорт данных

**Экран:**
1. Загрузка файла (drag-and-drop или кнопка выбора). Форматы: CSV, XLSX.
2. Предпросмотр первых 5 строк.
3. Маппинг колонок файла → полей системы.
4. Выбор режима: «Создать новых клиентов» / «Обновить существующих по ИНН».
5. Запуск импорта → прогресс-бар → отчёт об ошибках (строки с проблемами).

---

### 5.9. Настройки

**Разделы настроек (доступны CompanyAdmin):**
- **Компания** — название, логотип, часовой пояс, валюта, **язык по умолчанию**, API-ключ Контур-Фокус.
- **Пользователи** — см. 5.2.
- **Справочники** — управление элементами всех справочников (источники, ОПФ, тип отношений, продукты).
- **Воронки** — см. 5.3.
- **Персонализация** — пользователь может изменить свой язык интерфейса и тему оформления (также доступно через хедер).

---

### 5.10. Платформенная панель SuperAdmin

> **Добавлено в v1.2.** Отдельный интерфейс, недоступный обычным пользователям. Располагается на отдельном поддомене или пути (например, `/platform-admin`), защищён отдельной аутентификацией.

**Список тенантов:** таблица с колонками Название компании, Email владельца, Дата регистрации, Кол-во пользователей, Статус (активен / заблокирован).

**Действия:**
- Просмотр метаданных тенанта (без доступа к бизнес-данным — сделки, клиенты, контакты **не отображаются**).
- Блокировка / разблокировка тенанта с указанием причины.
- Поиск по названию компании или email.

---

## 6. Нефункциональные требования

> **Добавлено в v1.1. Дополнено в v1.3.**

| Параметр | Требование |
|---|---|
| **Производительность** | Список клиентов / сделок до 10 000 записей — отклик не более 2 секунд |
| **Браузерная поддержка** | Chrome 110+, Firefox 110+, Edge 110+, Safari 16+ |
| **Устройства** | Desktop (1280px+), планшет (768px+) |
| **PWA** | Приложение должно быть устанавливаемым (manifest.json, service worker). Lighthouse PWA-аудит ≥ 90. Offline-заглушка при отсутствии сети. Иконки 192×192 и 512×512. |
| **Мультиязычность** | Все строки UI вынесены в файлы переводов. На старте: ru, en. Добавление нового языка — только файл перевода, без изменений кода. |
| **Поддержка тем** | Светлая и тёмная темы через CSS custom properties. Переключение без перезагрузки страницы. Печать — только в светлой теме. |
| **Безопасность данных** | Изоляция тенантов на уровне каждого запроса к БД (`company_id` в WHERE) |
| **Доступность** | SLA определяется платформой GraviBase |
| **Масштаб** | Один тенант: до 50 пользователей, до 10 000 клиентов, до 50 000 сделок |

---

## 7. Интеграции

### 7.1. Контур-Фокус

- **Назначение:** автозаполнение реквизитов компании по ИНН.
- **Ключ:** каждый тенант использует собственный API-ключ (хранится в `companies.kontur_api_key`, зашифрован).
- **Поведение при ошибке:** показывается уведомление «Не удалось получить данные. Введите вручную». Система продолжает работу.
- **Кеш:** результат запроса сохраняется в `client_companies.kontur_data` (JSONB). Повторный запрос возможен вручную.

---

## 8. Требования к документированию

- **Руководство пользователя** (для роли Manager) — формат PDF, структура: работа с клиентами, сделками, отчётами, импортом.
- **Инструкция администратора** — настройка воронок, справочников, управление пользователями, настройка интеграции с Контур-Фокус.
- **Документация по структуре БД** — данное ТЗ (раздел 4).

---

*Документ подготовлен на основе ТЗ v1.0 с внесёнными исправлениями. Версия 1.3.*